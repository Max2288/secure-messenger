apiVersion: 1

providers:
  - name: "Default"
    folder: "Автоимпорт"
    type: file
    options:
      path: /var/lib/grafana/provisioning/dashboards
      recursive: true
      extensions: [json]
